

$(document).ready (function(e){

    //Récupère tous les éléments avec attributs data-jour sauf ceux avec valeur 0
$(`[data-jour][data-jour != 0]`);

    //Récupère l'élément avec attribut data-jour dont la valeur = 3
$(`[data-jour]:eq(3)]`);

});